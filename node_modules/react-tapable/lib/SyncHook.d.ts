import Hook from './Hook';
declare class SyncHook extends Hook {
    private fns;
    constructor(args: string[], name?: string);
    tapAsync(): void;
    tapPromise(): void;
}
export default SyncHook;
